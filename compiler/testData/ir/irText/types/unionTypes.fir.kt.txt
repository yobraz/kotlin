fun getSize(x: Any): Int {
  return { // BLOCK
    val tmp0_subject: Any = x
    when {
      tmp0_subject is Int -> x /*as Int */
      tmp0_subject is String -> x /*as String */.<get-length>() /*~> Unit */
      tmp0_subject is Boolean -> 1 /*~> Unit */
    }
  }
}

fun getRandomObject(): Any {
  return when {
    Random().nextBoolean() -> when {
      Random().nextBoolean() -> true
      else -> 7
    }
    else -> "fff"
  }
}

fun main() {
  val obj: Any = getRandomObject()
  getSize(x = obj) /*~> Unit */
  getSize(x = "foo") /*~> Unit */
  getSize(x = 1) /*~> Unit */
  error("") /* ErrorCallExpression */5.0;
}
